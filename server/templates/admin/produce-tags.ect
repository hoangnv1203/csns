<% extend 'admin/share/layout' %>

<%
	@pageTitle = 'Tags';

	@backUrl = @_url('admin.dashboard');

	createProduceCategoryUrl = @_url('admin.produce.tags.create');
%>

<% block 'breadcrumb' : %>
	<div class="ui breadcrumb">
		<a class="section" href="<%= @_url('admin.dashboard') %>">Admin</a>
		<i class="right angle icon divider"></i>
		<div class="active section"><%= @pageTitle %></div>
	</div>
<% end %>

<div class="ui container">
	<div class="ui borderless secondary compact menu">
		<div class="fitted header item">Tạo tag</div>
		<div class="fitted down item">
			<a href="<%= createProduceCategoryUrl %>">
				<i class="circular add icon"></i>
			</a>
		</div>
	</div>
	<% if @success.length > 0 : %>
		<div class="ui success message">
			<i class="close icon"></i>
			<div class="header"></div>
			<% for message in @success : %>
				<p><%= message %></p>
			<% end %>
		</div>
	<% end %>
	<div class="ui very relaxed divided list">
		<% if !@produceTags.length : %>
			<div class="item">
				<div class="content">
					<span>Bạn chưa có danh mục nông sản nào</span>
				</div>
			</div>
		<% end %>
		<% for tag in @produceTags : %>
			<%
				detailUrl = @_url('admin.produce.tags.detail', {
					id: tag._id,
				})
			%>
			<div class="item">
				<i class="large circular tag middle aligned icon"></i>
				<div class="middle aligned content">
					<a href="<%= detailUrl %>"><%= tag.name %></a>
				</div>
			</div>
		<% end %>
	</div>
</div>
<% block 'post-body' : %>
	<script src="/js/produce-tag-detail.js"></script>
<% end %>
